# vawsum_reactPOC
```
1. Check the script tag of package.json to start the application(if you are aware of package.json features).
2. In most of the cases, "npm start" works but if it doesn't work, use "npm run dev".

```
